# Sprint 6 — Crédits / billing

- `core/credits.ts` : table de coûts §F14, `costFor`, `canAfford`, `applyLedger`, `computeRollover` (report 25 %). Pur & testé.
- Stripe (checkout, webhooks) + `credit_ledger` DB à brancher.
- Coût affiché **avant** chaque action IA (règle §6).
